const events=document.querySelector('#events');function render(data){document.querySelector('#eventCount').textContent=data.events.length;document.querySelector('#highCount').textContent=data.events.filter(e=>e.level==='high').length;document.querySelector('#status').textContent=`${data.aiMode} AI · ${data.salesforceMode} Salesforce`;events.innerHTML=data.events.map(e=>`<article class="event"><div class="event-top"><div><h3>${e.company}</h3><p>${e.email}</p></div><span class="level ${e.level}">${e.level} intent · ${e.score}</span></div><p class="evidence">${e.evidence.join(' · ')}</p><div class="action"><span>Next action</span><strong>${e.recommendation}</strong></div></article>`).join('')}async function load(){render(await fetch('/api/events').then(r=>r.json()))}document.querySelector('#simulate').addEventListener('click',async()=>{document.querySelector('#status').textContent='Processing…';await fetch('/api/track',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:'new.visitor@example.com',company:'Demo Company',pages:['/pricing','/integrations'],minutes:7,visits:2})});await load()});load();

